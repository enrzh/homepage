// Gemini services have been removed as per user request.
// This file is kept as a placeholder to avoid build errors if imported elsewhere, 
// though all imports should have been removed.

export const searchWithGemini = async () => {
  throw new Error("Gemini services are disabled.");
};
